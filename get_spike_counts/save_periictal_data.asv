%% save peri=ictal data

%% Parameters
duration = 6*3600; % 6 hors

%% Paths
sz_path = '../../data/isolated_szs.csv';
eeg_path = '../../eeg_data/';
addpath('../../tools/');
paths = preictal_paths;

if ~exist("eeg_path","folder")
    mkdir(eeg_path)
end

%% Grab the isolated szs
T = readtable(sz_path);
T = T(T.isolated==1,:);
nszs = size(T,1);

% Loop over the szs
for i = 1:nszs

    %% 

end